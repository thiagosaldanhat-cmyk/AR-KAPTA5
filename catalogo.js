let models=[];const groupSelect=document.querySelector('#modelGroup'),sideField=document.querySelector('#sideField'),sideLabel=document.querySelector('#sideLabel'),sideSelect=document.querySelector('#side'),typeField=document.querySelector('#typeField'),typeSelect=document.querySelector('#type'),fenceField=document.querySelector('#fenceField'),fenceSelect=document.querySelector('#fence'),catalog=document.querySelector('#catalog'),resultTitle=document.querySelector('#resultTitle'),resultCount=document.querySelector('#resultCount'),clearButton=document.querySelector('#clearFilters');
function resetDependentFields(){sideSelect.value='';typeSelect.value='';fenceSelect.value=''}
function updateFields(){const g=groupSelect.value;sideField.hidden=true;typeField.hidden=true;fenceField.hidden=true;if(g==='esteira-90'){sideLabel.textContent='Lado da entrada';sideField.hidden=false;typeField.hidden=false;fenceField.hidden=false}if(g==='esteira-reta'){typeField.hidden=false;fenceField.hidden=false;if(typeSelect.value==='convertedora'){sideLabel.textContent='Lado do convertedor';sideField.hidden=false}}render()}
function filtered(){const g=groupSelect.value,s=sideSelect.value,t=typeSelect.value,f=fenceSelect.value;if(!g)return[];return models.filter(m=>{if(m.group!==g)return false;if(g==='sem-esteiras')return true;if(g==='esteira-90')return !!s&&!!t&&!!f&&m.side===s&&m.type===t&&m.fence===f;if(g==='esteira-reta'){if(!t||!f||m.type!==t||m.fence!==f)return false;if(t==='convertedora')return !!s&&m.side===s;return true}return false})}
function label(g){return{'sem-esteiras':'Sem esteiras','esteira-90':'Esteira 90º','esteira-reta':'Esteira reta'}[g]||g}
function render(){const g=groupSelect.value,items=filtered();if(!g){resultTitle.textContent='Selecione uma categoria';resultCount.textContent='';catalog.innerHTML='<div class="empty-state">Escolha o modelo de visualização para começar.</div>';return}const waiting=(g==='esteira-90'&&(!sideSelect.value||!typeSelect.value||!fenceSelect.value))||(g==='esteira-reta'&&(!typeSelect.value||!fenceSelect.value||(typeSelect.value==='convertedora'&&!sideSelect.value)));if(waiting){resultTitle.textContent='Complete as opções';resultCount.textContent='';catalog.innerHTML='<div class="empty-state">Preencha os campos acima para localizar o modelo correto.</div>';return}resultTitle.textContent=g==='sem-esteiras'?'Modelos sem esteira':'Modelo encontrado';resultCount.textContent=`${items.length} ${items.length===1?'modelo':'modelos'}`;catalog.innerHTML=items.map(m=>`<article class="card ${m.available?'':'unavailable'}"><div class="visual"><span class="number">${m.number}</span><div class="machine"></div>${m.available?'':'<span class="coming-soon">Em breve</span>'}</div><div class="body"><span class="pill">${label(m.group)}</span><h3>${m.name}</h3>${m.available?`<a class="primary" href="visualizador.html?modelo=${encodeURIComponent(m.id)}">Visualizar modelo</a>`:'<button class="disabled-button" disabled>Modelo ainda não disponível</button>'}</div></article>`).join('')}
groupSelect.addEventListener('change',()=>{resetDependentFields();updateFields()});typeSelect.addEventListener('change',updateFields);sideSelect.addEventListener('change',render);fenceSelect.addEventListener('change',render);clearButton.addEventListener('click',()=>{groupSelect.value='';resetDependentFields();updateFields()});fetch('modelos.json').then(r=>r.json()).then(d=>{models=d;render()});