const id=new URLSearchParams(location.search).get('modelo')||'01-base-3d';const viewer=document.querySelector('#model');fetch('modelos.json').then(r=>r.json()).then(models=>{const m=models.find(x=>x.id===id)||models[0];document.title=`${m.name} — KAPTA AR`;document.querySelector('#name').textContent=m.name;document.querySelector('#category').textContent=m.group==='sem-esteiras'?'Sem esteiras':m.group==='esteira-90'?'Esteira 90º':'Esteira reta';viewer.src=`modelos/${m.file}`});viewer.addEventListener('progress',e=>{const fill=document.querySelector('.progress-fill');fill.style.width=`${e.detail.totalProgress*100}%`;if(e.detail.totalProgress===1)document.querySelector('.progress').style.display='none'});